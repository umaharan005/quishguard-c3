# Laptop: everything needed to run the scan app (python -m quishguard.app)
# 1) Install PyTorch first (CPU is enough):
#      pip install torch --index-url https://download.pytorch.org/whl/cpu
# 2) Then:  pip install -r requirements-app.txt  and  pip install -e .
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
opencv-python>=4.9
Pillow>=10.0
tldextract>=5.1
pyzbar>=0.1.9
xgboost>=2.0
timm>=1.0
joblib>=1.3
fastapi>=0.110
uvicorn>=0.29
segno>=1.6
zxing-cpp>=2.2      # strongest decoder for real photos / designer codes
